Player Classes:
Paladin(!)
	Paladin Abilites:
		Heal(!)
		Consecrate(?)
		Condemn(?)
		Smite(?)
Mage(!)
	Mage Abilities:
		Fire(?)
		Ice(?)
		Become Etheral(?)
		
Warrior(!)
	Warrior Abilities:
		Slash(!) 	// Standard slash attack. //
		Thrust(!) 	// Does less damage but critical hits do more than ususal. If performed after a stagger, is an automatic crit (parry). //
		Stagger(!)	// Swings shield at enemy. Does very little damage but has a decent chance to stun for one turn. //
		Gaurd(!) 	// Raises shield. The enemy's next attack will do significantly less damage. Has a slight chance to counter the attack. //
Deprived(!)
	Deprived Abilities:
		Club(!)		// Slams the enemy with a club doing a large amount of damage. //
		Kick(!)		// Kicks the enemy for some damage and a change to be stunned (knocked over). //
		Roll(!)		// The enemy's next move is significantly less likely to hit. //
Ranger(!)
	Ranger Abilities:
Rogue(!)
	Rogue Abilities:
		Execute(!)	// Does massive damage on a critical hit but does nothing otherwise. If the enemy is below 20% health, kills them instantly and is guaranteed to hit. //
		Multi Strike(!)	// Hits the enemy three times with a higher chance for a critical hit on the third. //
		Lunge(!)	// Hits the enemy once, hard, and stuns them for one turn. If done twice in a row, will not stun the second time. //
		Steady(!)	// Increases the chance of a crtitical hit for a few turns. //
Enemy Classes:
Warlock(!)
	Warlock Abilities:
		Shadow Bolt(!)
		Into Shadows(!)
		Immolate(!)
Barbarian(!)
	Barbarian Abilities:
		Shield Slam(!)
		Charge(!)
		Block(!)
		Battle Cry(!)
Hunter(!)
	Hunter Abilities:
		Poison Shot(!)
		Power Shot(!)
		Camouflage(!)
Fighter(!)
	Fighter Abilities:
		Jab(!)
		Uppercut(!)
		Dodge(!)
Assassin(!)
	Assassin Abilities:
		Poison(!)
		Backstab(!)
		Vanish(!)
Necromancer(!)
	Necromancer Abilities:
		Raise Pet(!)
		Curse(!)
		Swarm(!)

Pets:
Wolf(!)
Boar(!)
Hawk(!)
Imps(!)
Undead(!)

Balancing Issues:
Class Abilities(!)
Class Health/Energy(!)
Item stats by level(!)
Enemy pets(!)

Enemy AI:
Which spells at which times(!)
When to use items(!)
Change strategy when pet dies(!)

Battle Handler:

